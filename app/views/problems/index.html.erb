<h1>Listing problems</h1>

<table>
  <tr>
    <th>Problem (click to answer)</th>
    <th>Step count</th>
  </tr>

<% @problems.each do |problem| %>
  <tr>
    <td><%= link_to problem.label, answer_problem_path(problem) %></td>
    <td><%= problem.step_count %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Problem', new_problem_path %>
